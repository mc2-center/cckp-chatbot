{
  "messageVersion": "1.0",
  "agent": {
    "name": "cckp-kg-agent",
    "version": "test"
  },
  "actionGroup": "cckpGraphRag",
  "function": "sparqlQuery",
  "parameters": [
    {
      "name": "query",
      "type": "string",
      "value": "SELECT ?type (COUNT(?s) AS ?count) WHERE { ?s a ?type } GROUP BY ?type ORDER BY DESC(?count) LIMIT 5"
    }
  ]
}
